name: HaC
description: Defines the infrastructure for my homelab
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
packages:
  ansible:
    source: terraform-provider
    version: 0.12.0
    parameters:
      - ansible/ansible
