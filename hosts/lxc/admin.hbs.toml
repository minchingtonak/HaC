# verified
id = 213
hostname = "admin"

description = "homelab administration-related apps"

[stacks]
traefik = {}
dozzle = {}
apprise = {}
semaphore = {}
# komodo = {}
# glance = {}

[cpu]
cores = 2

[memory]
dedicated = 2048
swap = 1024

[disk]
size = 8

[[mountPoints]]
volume = "{{{@pve.lxc.appDataDirectory}}}/admin"
mountPoint = "/docker"

[[provisioners]]
type = "ansible"
playbook = "provisioners/ansible/lxc-init.yaml"
replayable = false

[[provisioners]]
type = "script"
script = "provisioners/scripts/install-docker.sh"
