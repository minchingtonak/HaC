services:
  dozzle:
    image: amir20/dozzle:v9.0.1@sha256:a7529f02748a9520a4f372f7b349e099bb0bc6ae93c2be9d85945f51d45f8967
    container_name: dozzle
    restart: unless-stopped
    volumes:
      - ./users.yaml:/data/users.yaml
      - /var/run/docker.sock:/var/run/docker.sock
    # ports:
    #   - 8080:8080
    environment:
      - DOZZLE_ENABLE_ACTIONS=true
      - DOZZLE_ENABLE_SHELL=true
      # - DOZZLE_AUTH_PROVIDER=simple # uncomment to enable username/pw auth
      - DOZZLE_NO_ANALYTICS
    labels:
      - traefik.enable=true
      - traefik.http.routers.dozzle.rule=Host(`{{{domainForApp "dozzle"}}}`)
      - traefik.http.routers.dozzle.entrypoints=https
      - traefik.http.routers.dozzle.tls=true
      - traefik.http.routers.dozzle.tls.certresolver=porkbun
      - traefik.http.routers.dozzle.service=dozzle
      - traefik.http.services.dozzle.loadbalancer.server.port=8080
      - traefik.http.routers.dozzle.middlewares=oidc-auth@file
    networks:
      - traefik

networks:
  traefik:
    external: true
