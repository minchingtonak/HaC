services:
  homarr:
    container_name: homarr
    image: ghcr.io/homarr-labs/homarr:v1.40.0@sha256:bac5842539316f80b96c15ce90abd8614c0e254cd5f33244a0de7fdde064ab7d
    restart: unless-stopped
    volumes:
      - /docker/homarr:/appdata
    environment:
      - SECRET_ENCRYPTION_KEY={{{SECRET_ENCRYPTION_KEY}}}
    # ports:
    #   - '7575:7575'
    labels:
      - traefik.enable=true
      - traefik.http.routers.homarr.rule=Host(`{{{domainForApp "homarr"}}}`)
      - traefik.http.routers.homarr.entrypoints=https
      - traefik.http.routers.homarr.tls=true
      - traefik.http.routers.homarr.tls.certresolver=porkbun
      - traefik.http.routers.homarr.service=homarr
      - traefik.http.services.homarr.loadbalancer.server.port=7575
    networks:
      - traefik

networks:
  traefik:
    external: true
